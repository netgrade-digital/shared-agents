# OpenCode

Auto-configured when `~/.config/opencode` exists.

## Global instructions

`install.sh` merges into `~/.config/opencode/AGENTS.md`.

## Sync

Agent runs `$SHARED_AGENTS_HOME/scripts/sync.sh pull` as first action each session.

## Skills

Symlinked to `~/.config/opencode/skills/` when present.
